{
  "name": "ish-project-dashboard",
  "version": "1.0.0",
  "description": "Increment Service Hub - DevOps Project Dashboard",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "healthz": "curl -fsS http://localhost:8080/healthz || exit 1"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}

